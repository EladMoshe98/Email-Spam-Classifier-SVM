Spam / Ham Classifier
This notebook implements a spam email classifier using Support Vector Machines (SVM) with a Bag of Words model. To compare with the SVM implementation there are some alternative classifiers created.
